"use client";

/**
 * @deprecated Use AppProviders from app-providers.tsx (loaded in layout).
 * Kept for backwards compatibility with any stray imports.
 */
export { AppProviders as WalletProvider } from "./app-providers";
