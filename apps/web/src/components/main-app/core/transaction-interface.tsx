// Legacy transaction-interface — replaced by SwapInterface in /components/swap/
// Kept as a re-export shim to avoid breaking any remaining imports.
export { SwapInterface as TransactionInterface } from "@/components/swap/swap-interface-loader";
